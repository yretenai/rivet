project('clipp',
    'cpp',
    version : '1.2.4', # bump from 1.2.3 because we're using a forked version and it's non-compatible due to C++20 removals
    license : 'MIT'
)

clipp_dep = declare_dependency(
    include_directories : include_directories('include')
)
